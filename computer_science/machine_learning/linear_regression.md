Linear Regression
=================
# Linear Models

$$
y = w^T x + b
$$

* We can combine the bias term $b$ with the weight vector $w$ by prepending an extra term $x_0=1$ to every input vector.In this case, our model reduces to $y=w^Tx$. 

